import styled from "styled-components";

const PlayAgainButton = styled.button`
   background: seaGreen;
   color: white;
   border: none;
   border-radius: 3px;
   font-family: 'Karla', sans-serif;
   font-weight: 400;
   font-size: 1.4rem;

   &:hover, 
   &:focus {
    background: #468c65;
   }
`

// PlayAgainContainer = styled.div`
    
// `

export { PlayAgainButton }